
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Void Canvas application, owning projects and published sites.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user, used for identification. (Authentication handled externally)",
          "format": "email"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the user account was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "createdAt",
        "updatedAt"
      ]
    },
    "Project": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Project",
      "type": "object",
      "description": "Represents a creative project developed by a user on the Void Canvas, which can be published to a subdomain.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Project entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who owns this project. (Relationship: User 1:N Project)"
        },
        "name": {
          "type": "string",
          "description": "A user-defined name for the project or site."
        },
        "description": {
          "type": "string",
          "description": "An optional, user-provided description for the project."
        },
        "status": {
          "type": "string",
          "description": "The current lifecycle status of the project (e.g., 'draft', 'published', 'archived')."
        },
        "subdomainName": {
          "type": "string",
          "description": "The unique part of the subdomain assigned to the published project (e.g., 'myproject' for 'myproject.voidcanvas.com'). This value is set when the project's status becomes 'published'."
        },
        "contentJson": {
          "type": "string",
          "description": "A JSON string representing the actual content and configuration of the site. Photos are now excluded and stored in the media subcollection."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the project was initially created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the project was last modified.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "name",
        "status",
        "contentJson",
        "createdAt",
        "updatedAt"
      ]
    },
    "ProjectMedia": {
      "title": "ProjectMedia",
      "type": "object",
      "description": "Stores individual photos as Base64 to allow each photo to reach the 1MB document limit.",
      "properties": {
        "id": { "type": "string" },
        "base64": { "type": "string", "description": "The image data as Base64 string." },
        "type": { "type": "string", "enum": ["album", "spotify", "memory", "journey"] }
      }
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profile information."
        }
      },
      {
        "path": "/published_sites/{subdomainName}",
        "definition": {
          "entityName": "Project",
          "schema": {
            "$ref": "#/backend/entities/Project"
          },
          "description": "Stores project data. Publicly readable."
        }
      },
      {
        "path": "/published_sites/{subdomainName}/media/{mediaId}",
        "definition": {
          "entityName": "ProjectMedia",
          "schema": {
            "$ref": "#/backend/entities/ProjectMedia"
          },
          "description": "Stores individual photos for the site."
        }
      }
    ]
  }
}
